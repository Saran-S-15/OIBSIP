import React from 'react'
import { Loader } from "lucide-react"

const Spinner = () => {
    return (
        <Loader className='animate-spin flex justify-center items-center mx-auto' />
    )
}

export default Spinner